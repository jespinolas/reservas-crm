import { InstagramConnectionCard } from "@/components/settings/instagram-connection-card";

export const dynamic = "force-dynamic";

export default function InstagramSettingsPage() {
  return <InstagramConnectionCard />;
}
