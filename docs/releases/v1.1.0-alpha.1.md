# Release Evidence: v1.1.0-alpha.1

## Summary

- Version: `v1.1.0-alpha.1`
- Release type: alpha
- Date: 2026-07-22
- Commit: pending final tagged commit
- Tag: pending `v1.1.0-alpha.1`
- Maintainer: `jespinolas`
- Reviewer: `jespinolas` for owner approval; independent GitHub review depends
  on future bot or collaborator workflow

## Verification

| Check | Command Or Source | Result | Notes |
| --- | --- | --- | --- |
| Test | `pnpm test` | Passed | 31 test files, 160 tests passed on the release-prep branch. |
| Typecheck | `pnpm typecheck` | Passed | Passed after `pnpm build` regenerated Next type files. GitHub CI also passed on `main`. |
| Lint | `pnpm lint` | Passed | Passed on the release-prep branch and in GitHub CI. |
| Build | `pnpm build` | Passed | Passed on the release-prep branch and in GitHub CI. Existing Tailwind ambiguity warning for `duration-[220ms]` remains non-blocking. |
| Docker build | `docker build -t reservas-crm:v1.1.0-alpha.1 .` | Passed | Local image ID `sha256:8b5090209a2828d6a61c1473fb40a28fa54af4a4fe9130189c9bd9ee7871236c`. |
| Docker runtime | `docker run --rm reservas-crm:v1.1.0-alpha.1 node --version` | Passed | Returned `v22.23.1`. |
| Compose config | `docker compose config --quiet` with placeholder production values | Passed | Compose configuration is syntactically valid with documented placeholders. |
| Fresh install smoke | `docs/runbooks/fresh-install-smoke-test.md` | Not run | Full clean-host runtime smoke remains pending for the prerelease publication gate. |
| Secret scan | Repository hygiene checklist | Passed | Targeted scans found no known token, private-key, `.env`, or AI/tooling artifacts outside the public hygiene runbook. |
| GitHub CI | `CI` workflow on `main` | Passed | `Test`, `Typecheck`, `Lint`, and `Build` passed on merge commit `c7ce7e0`. |

## Screenshots

- Required: yes
- Reason: public README and repository presentation include current branded UI
  screenshots.
- Files:
  - `docs/screenshots/reservas-inbox.png`
  - `docs/screenshots/reservas-reservations.png`
  - `docs/screenshots/reservas-settings-about.png`

## Upgrade Notes

- Migrations: No database migration.
- Environment variables: No new required environment variables.
- Manual steps: Existing self-hosted installations should review
  `SECURITY.md`, `docs/deployment/docker-compose.md`, and
  `docs/runbooks/fresh-install-smoke-test.md`.
- Rollback limitations: This prerelease is repository/governance focused. If a
  deployment consumes it and needs rollback, redeploy the previous known-good
  application image or source commit.

## Known Limitations

- This alpha primarily validates the public repository foundation, not a new
  end-user feature release.
- Fresh install smoke test documentation exists, but a full clean-host smoke
  result is still required before treating this as a production-ready release.
- GHCR publishing is expected to run after the tag is pushed. Image digest and
  package URL must be added after publication.
- Future automated pull requests should be opened by a separate bot or machine-user
  identity so `jespinolas` can approve manually in GitHub.

## Security Notes

- Secret handling: public docs use placeholders only; usable credentials must
  remain outside the repository.
- Webhook posture: production WhatsApp webhooks require HTTPS, unique verify
  tokens, and `META_APP_SECRET` signature validation.
- Token/encryption posture: WhatsApp and integration tokens must be stored
  encrypted at rest with installation-specific secrets.
- Vulnerability disclosure notes: report vulnerabilities privately through
  `SECURITY.md`; do not open public issues containing exploit details or
  secrets.
- Repository controls: vulnerability alerts, Dependabot security updates,
  secret scanning, secret scanning push protection, CI, labels, and protected
  `main` are enabled.

## GHCR Evidence

- Required: yes
- Image tags: pending `ghcr.io/jespinolas/reservas-crm:v1.1.0-alpha.1`
- Digest: pending tag workflow result
- Workflow run: pending tag workflow result
- Did `latest` change: no; prerelease tags must not update `latest`

## Changelog And Release Notes

- Changelog section: `CHANGELOG.md#110-alpha1---2026-07-22`
- GitHub release URL: pending
- Pull requests included:
  - `#6` public repository foundation

## Approval

- Owner approval: requested by the maintainer on 2026-07-22 for prerelease
  preparation work, with production actions still excluded.
- Reviewer approval: pending future GitHub identity workflow or collaborator
  review for ordinary feature PRs.
- Exceptions approved: fresh install smoke and GHCR digest are pending final
  prerelease publication evidence and must be completed or documented in the
  GitHub release notes.
